version: '3.4'

services:
  tictactoe.api:
    image: ${DOCKER_REGISTRY-}tictactoeapi
    build:
      context: .
      dockerfile: TicTacToe.Api/Dockerfile
    
    ports:
     - "32000:80"
     - "32005:480"